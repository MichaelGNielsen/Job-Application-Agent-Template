/**
 * System-wide configurations and constants (v4.8.5).
 */
module.exports = {
    // 100MB buffer for exec calls to handle large Gemini CLI / Pandoc outputs
    MAX_EXEC_BUFFER: 1024 * 1024 * 100
};
